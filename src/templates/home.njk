---
layout: main.njk

relatedPages:
  - surfschool
  - mountainBike
  - kidsAreWelcome
  - happyHorses
  - bedAndBreakfast

topTips:
  - kidsAreWelcome
  - happyHorses
  - mountainBike
bookingBanner:
  - bookingBedAndBreakfast
  - bookingPackages
  - bookingExperiences

---

{% import "hero.njk" as hero %}
{% import "related.njk" as related %}
{% import "partial.njk" as partial %}
{% import "gallery.njk" as gallery %}
{% import "video.njk" as video %}
{% import "columns.njk" as columns %}
{% import "booking-columns.njk" as bookingColumns %}

{% image {src:"./src/assets/home/hero/portuguese-spirit-of-surfing.jpg", 
alt:"Da Silva Surfcamp Mountain Bike Portugal", myClass:"max-scaled"}%}

<section class="centered">
  {{ partial.get(collections, tags, "welcome", locale) }}
  {{ partial.get(collections, tags, "corona", locale) }}
</section>

<nav class="display-if-middle-to-wide">
  {% include "masonry.njk" %}
</nav>
<nav class="display-if-small">
  {{ related.three(links[locale], relatedPages)}}
</nav>

<section>
  <div class="centered">
    {{ partial.get(collections, tags, "spirit-of-surfing", locale) }}
  </div>
  {{ video.vimeo(videoId="173051720") }}
</section>

<section class="centered">
  {{ partial.get(collections, tags, "qualified-school", locale) }}
</section>

<section>
  {{ related.three(links[locale], bookingBanner)}}
</section>

<section>
  {{ bookingColumns.threeCards(
    headline="Booking",
    leftTitle=links[locale].bookingRequest.title,
    leftLink=links[locale].bookingRequest.path,
    leftColumn=partial.get(collections, tags, "booking-request", locale),
    middleTitle=links[locale].prices.title,
    middleLink=links[locale].prices.path,
    middleColumn=partial.get(collections, tags, "prices", locale),
    rightTitle=links[locale].travelDirections.title,
    rightLink=links[locale].travelDirections.path,
    rightColumn=partial.get(collections, tags, "travel-directions", locale)
  )}}
</section>

<section class="centered">
  {{ related.three(links[locale], topTips)}}
  {{ related.three(links[locale], bookingBanner)}}
</section>